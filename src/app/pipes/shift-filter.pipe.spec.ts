import { ShiftFilterPipe } from './shift-filter.pipe';

describe('ShiftFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ShiftFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
