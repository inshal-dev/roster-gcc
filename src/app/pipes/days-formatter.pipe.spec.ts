import { DaysFormatterPipe } from './days-formatter.pipe';

describe('DaysFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new DaysFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
