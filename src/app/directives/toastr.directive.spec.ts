import { ToastrDirective } from './toastr.directive';

describe('ToastrDirective', () => {
  it('should create an instance', () => {
    const directive = new ToastrDirective();
    expect(directive).toBeTruthy();
  });
});
